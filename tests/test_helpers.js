// Generated by LiveScript 1.2.0
describe('utils.camelize', function(){
  var x$;
  x$ = it;
  x$('should camelCase dash-separted names', function(){
    expect(utils.camelize('foo-bar')).toBe('fooBar');
  });
  x$('should camelCase with more than one dash', function(){
    expect(utils.camelize('foo-bar-baz')).toBe('fooBarBaz');
  });
  x$('should work with underscores', function(){
    expect(utils.camelize('foo_bar')).toBe('fooBar');
  });
  x$('should work with mixed underscore and dash', function(){
    expect(utils.camelize('foo_bar-baz')).toBe('fooBarBaz');
  });
});